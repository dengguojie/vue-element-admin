# source file:python/te/lang/base/shape_classifier.py

class Mode(Enum):
    NONE = auto()
    ELEWISE = auto()
    ELEWISE_WITH_BROADCAST = auto()
    REDUCE = auto()

MODE_TO_STRING_MAP = {
    Mode.NONE: "",
    Mode.ELEWISE: "elewise",
    Mode.ELEWISE_WITH_BROADCAST: "broadcast",
    Mode.REDUCE: "reduce"
}

def classify(ins: list, mode: Mode = Mode.NONE):
